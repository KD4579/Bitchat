# Trading Bot Configuration
# Copy this to .env and fill in your values

# Bot wallet private key (from MetaMask - NEVER commit this)
BOT_PRIVATE_KEY=0x_YOUR_PRIVATE_KEY_HERE

# Database (same as nodejs/config.json)
DB_HOST=localhost
DB_USER=KamalDave_bitchat
DB_PASS=
DB_NAME=KamalDave_bitchat

# BSC RPC (override, or leave empty to use admin panel setting)
RPC_URL=
